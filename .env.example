# Copy to .env.local and fill in. Do not commit .env or .env.local.

# Database (Neon Postgres — coming in phase 0, step 4)
# DATABASE_URL=

# Auth (Auth.js v5 — coming in phase 0, step 6)
# AUTH_SECRET=
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
